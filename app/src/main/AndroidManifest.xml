<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.CAMERA" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" android:maxSdkVersion="32" />
    <uses-permission android:name="android.permission.READ_MEDIA_IMAGES" />

    <application
        android:name=".RecetarioApp"
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.RecetarioBoliviano">
        <activity
            android:name=".vista.actividades.SplashActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity android:name=".vista.actividades.MainActivity" />
        <activity android:name=".vista.actividades.LoginActivity" />
        <activity android:name=".vista.actividades.RegistroActivity" />
        <activity android:name=".vista.actividades.PerfilActivity" />
        <activity android:name=".vista.actividades.AdminActivity" />
        <activity android:name=".vista.actividades.RecetaFormActivity" />
        <activity android:name=".vista.actividades.RecetaDetalleActivity" />
        <activity android:name=".vista.actividades.GestionRecetasActivity" />
        <activity android:name=".vista.actividades.AdminUsuarioDetalleActivity" />
        <activity android:name=".vista.actividades.AdminPlaylistDetalleActivity" />
        <activity android:name=".vista.actividades.PlaylistActivity" />
        <activity android:name=".vista.actividades.RecetasDepartamentoActivity" />

        <provider
            android:name="androidx.core.content.FileProvider"
            android:authorities="${applicationId}.fileprovider"
            android:exported="false"
            android:grantUriPermissions="true">
            <meta-data
                android:name="android.support.FILE_PROVIDER_PATHS"
                android:resource="@xml/file_paths" />
        </provider>
    </application>

</manifest>